document.addEventListener('DOMContentLoaded', () => {
    const canvas = document.getElementById('canvas');
    // Initialize draggable components, etc.
    console.log('Interactive Wireframe Prototyper Loaded');
});