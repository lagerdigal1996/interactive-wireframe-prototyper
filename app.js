document.addEventListener('DOMContentLoaded', () => {
    const canvas = document.getElementById('canvas');
    // Initialize draggable components and set up event listeners for the canvas
    console.log('Interactive Wireframe Prototyper Loaded');
});